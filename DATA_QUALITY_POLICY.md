# データ品質の方針（Settle in Japan）

このサイトが扱うのは、**間違えると人の生活に実害が出る情報**（在留資格、届出の期限、手続き）。
他3サイトより厳しく運用する。

## 大原則

1. **公式ページの本文に書かれていないことは書かない。** 出入国在留管理庁・厚生労働省・自治体など、
   制度を所管する公式ページで確認できたものだけを載せる
2. **確認できないことは埋めない。** `null` または `unknown` のままにして、`review_flags` に理由を残す。
   「たぶんこうだろう」で埋めない
3. **出典と最終確認日を必ず持たせる。** 制度が変わる分野なので、「いつ時点の情報か」が読者にとっての信頼の根拠になる
4. **プロンプトのルールは、必ず機械的なチェックとしても実装する。** AIに「本文にないことを書かないで」と
   指示するだけでは守られない（既存3サイトで実例が複数あった）

## 「本文照合」のしくみ

このリポジトリの中心的なガード。

1. `fetch-official.yml`（GitHub Actions）が、`data/sources.json` に登録された公式ページを取得し、
   本文を `data/raw/<id>.txt` に保存する
2. `data/visa-types.json` に書く文言（在留期間・活動内容）は、**その本文にある表現をそのまま使う**
3. `npm test` が、書いた文言が保存された本文に**実在するか**を照合する

言い換えると照合に落ちる。**それが狙い**。要約したくなったら、要約は別項目（`summary_ja` など）に分け、
事実の記述そのものは公式の文言を保つ。

## 現在のガード一覧

| ガード | 何を防ぐか | 実装 |
|---|---|---|
| スキーマ検証 | 項目の欠落・想定外の値 | `scraper/lib/schema.js` + `npm run validate` |
| 出典の必須化 | 出典なしのレコードが混じる | `scraper/test/visa-types.test.js` |
| 最終確認日の必須化 | いつの情報か分からなくなる | 同上 |
| 本文照合 | 公式ページに無い文言をAIが作る | `scraper/lib/verify.js` + `scraper/test/verify.test.js` |
| 出典URLの制限 | 個人ブログ・まとめサイトを出典にしてしまう | `scraper/lib/schema.js` の `OFFICIAL_HOSTS` |
| 断定表現の検出 | 個別の相談に答える文章が混じる | `scraper/test/wording.test.js` |

## 新しい項目を足すときの手順

1. `scraper/lib/schema.js` に項目と許容値を足す
2. **その項目を公式ページの本文と照合する方法を決める**（できないなら、その項目は持たない）
3. テストを足す
4. 既存の全レコードに同じチェックをかけ直す

順番を変えないこと。先にデータを入れてから照合方法を考えると、結局照合できないまま残る。

## 確認できないまま残している項目（2026-09-16 時点）

| 項目 | 件数 | 調べた結果 |
|---|---|---|
| 就労の可否（`work_allowed: "unknown"`） | 5件 | 特定活動・永住者・日本人の配偶者等・永住者の配偶者等・定住者 |

**調べたこと**：取得済みの公式ページ（在留資格一覧表・各在留資格のページ・生活就労ガイドブックの入口）を
本文検索したが、これらの在留資格について「就労に制限がない」旨を述べた**文言は見つからなかった**。
`生活・就労ガイドブック` は入口ページだけがHTMLで、本体はPDFのため取得していない
（`fetch-official.js` はPDFを対象外にしている）。

**したがって `unknown` のまま残す。** 一般的な知識としては就労制限がないとされるが、
このリポジトリは「保存した公式ページの本文に実在する文言だけを載せる」方針なので、
裏の取れないことは書かない。これを埋めるには、そう書かれている公式のHTMLページを
`data/sources.json` に登録して取得する必要がある。
