# CLAUDE.md

このリポジトリ（Settle in Japan / 外国人の日本移住ポータル）で作業するときは、
**まず次の3つを読むこと。**

1. **[PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)** — 図鑑シリーズ共通の背景、運営者（Tatsuroさん）について、
   確認すべきこと／不要なこと、データ品質の原則
2. **[DECISIONS.md](DECISIONS.md)** — Tatsuroさんがこれまでに決めたこと。
   **ここにある判断を覆す提案をする前に、理由と「見直す条件」を必ず読むこと**
3. **[DATA_QUALITY_POLICY.md](DATA_QUALITY_POLICY.md)** — 公式情報の扱い方と、現在のガード一覧

このサイトは図鑑シリーズの4サイト目（既存は転職エージェント図鑑・フリーランス案件図鑑・スキルアップ図鑑）。
運営の型は同じだが、**扱う情報が法律・行政手続きであるため、他3サイトより厳しい線引きがある**（下記）。

## 特に外してはいけない点

- **Tatsuroさんはエンジニアではない。** 報告・質問は平易な言葉で。「何のための変更か」を一言でまとめてから、
  判断に必要な範囲だけ説明する
- **個別の相談に答えない。** 「あなたの場合はこのビザが取れます」と答えることは、行政書士・弁護士の資格が
  必要な領域に踏み込むおそれがある。サイトは**制度の説明と、公式窓口への案内に徹する**
  - ○「この在留資格の要件は、出入国在留管理庁のページによるとこうです（出典）」
  - ×「あなたは要件を満たしているので申請できます」
- **公式ページに書かれていないことは、AIに絶対に作らせない。** 制度・期間・金額・日付は、出入国在留管理庁・
  厚生労働省・自治体など**公式ページの本文で確認できたものだけ**を書き、出典と最終確認日を必ず載せる
- **確認できない情報は埋めない。** null のままにして `review_flags` に理由を残す
- **プロンプトに書いたルールは、必ず機械的なチェックとしても実装する。** このサイトでは
  「記載した文言が、保存した公式ページの本文に実在するか」を `npm test` で照合している

## 情報の集め方（クラウドからは公式サイトに行けない）

クラウドのセッションは許可ドメインしか見られず、**`moj.go.jp` などの公式サイトには直接アクセスできない**。
そのため次の2段構えにしている。

1. **GitHub Actions が公式ページを取得する**（Actions からは外部サイトに出られる）
   `.github/workflows/fetch-official.yml` を手動実行すると、`data/sources.json` に登録した公式ページを
   取得し、本文を `data/raw/<id>.txt` に保存してコミットする
2. **保存された本文を読んでデータを作る**
   クラウドのセッションはリポジトリ内の `data/raw/` を読めるので、そこから
   `data/visa-types.json` を組み立てる。**本文に無いことは書かない**

この形なら、書いた内容が公式ページの本文と一致しているかを、あとから何度でも機械的に確かめられる。

## データの線引き

- **在留資格図鑑**（最初に作るもの）：`data/visa-types.json`。
  スキーマは [scraper/lib/schema.js](scraper/lib/schema.js) の1か所で決める
- **出典**：すべてのレコードに `source_url`・`source_checked_at`（最終確認日）が必要。
  これが無いレコードはテストで落ちる
- **在留期間・活動内容**：公式ページの本文にある文言をそのまま使う。言い換えない
  （言い換えると、照合テストが通らなくなる。それが狙い）
- **就労の可否・家族帯同の可否**：公式ページで確認できたときだけ `yes` / `no` を入れる。
  確認できないときは `unknown` のままにして `review_flags` に理由を残す

## よく使うコマンド

```bash
cd scraper
npm test            # ユニットテスト（データの形・出典・本文照合）
npm run validate    # data/visa-types.json をスキーマ検証
npm run fetch       # 公式ページを取得して data/raw/ に保存（※通信が必要。ふつうは Actions で動かす）
npm run verify-live # 公開サイトがリポジトリどおりか確認（main への反映が終わったあと）
```

依存パッケージは使っていない（Node 22 の標準機能だけ）。`npm install` は不要。

## クラウド（Claude Code on the web）で作業するとき

- **読めるのは、このリポジトリにコミットされているファイルだけ**
- **`git push` はセッションの作業ブランチにしかできない。** 変更は PR にまとめ、Tatsuroさんがマージして公開される
- **通信できるのは許可ドメインだけ。** 公式ページを確認できなかったときは、推測で埋めずにそう報告する
- 公式ページの取得は `fetch-official.yml` を実行して、結果をリポジトリから読む

## 作業を終えるときの確認

- `npm test` が通っているか
- 新しい項目を足したなら、本文照合のガードとテストも足したか
- 公式ページを取り直したなら、`source_checked_at` を更新したか
- Tatsuroさんが新しく判断したことがあれば、`DECISIONS.md` に日付つきで追記したか
