# 判断の記録（Settle in Japan）

Tatsuroさんが決めたこと、方針として固めたことを「いつ・何を・なぜ」の形で残す。
**ここにある判断を覆す提案をする前に、理由と「見直す条件」を読むこと。** 見直す条件に当てはまらないのに
同じ提案を繰り返さない。新しい判断をしたら、日付つきで追記する（古い判断は消さずに「見直し済み」と書き足す）。

図鑑シリーズ共通の背景は [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)、公式情報の扱い方は
[DATA_QUALITY_POLICY.md](DATA_QUALITY_POLICY.md) にある。

---

## 現在の状況（2026-09-15 時点）

- **立ち上げ直後。** サイトはまだ公開していない。このリポジトリは方針とデータの土台だけがある状態
- **ドメイン**：`settle-in-japan.net`（2026-09-15 取得済み。DNS の設定はこれから）
- **アクセス解析**：GA4 測定ID `G-44PECD16GK`
- **広告**：AdSense は既存サイトのID `ca-pub-5761092657360295` を流用
- **最初に作るもの**：在留資格図鑑（比較表）
- 既存3サイト（転職エージェント図鑑・フリーランス案件図鑑・スキルアップ図鑑）は運用中

---

## サイトの方針

### サイト名は「Settle in Japan」にする（2026-09-15）
- **決めたこと**：サイト名を `Settle in Japan` とする。日本語で呼ぶときは「日本移住ガイド」
- **理由**：`settle` は「腰を落ち着ける・定住する」の意味で、観光でも短期滞在でもなく
  「暮らしはじめる人のための場所」だと一語で伝わる。"settle in japan" "moving to japan" という
  検索語でも拾われる。候補には Tadaima Japan・Japan Compass などもあったが、
  ブランド性より**検索で見つかること**を優先した
- **見直す条件**：ドメインが取得できなかったとき

### ドメインは settle-in-japan.net にする（2026-09-15）
- **決めたこと**：`settle-in-japan.net` を取得した。既存3サイト（agent-zukan.net / freelance-anken-zukan.net /
  skillup-zukan.net）と同じ `.net` で揃えた
- **理由**：サイト名がそのままドメインになるので、覚えやすく、検索でも一致する。
  図鑑シリーズと同じ `.net` にすることで、運営者側の管理も揃う

### トップページは「在留資格」ではなく「時期」で切る（2026-09-15）
- **決めたこと**：入口を5つにする。来日前 ／ 来日直後（〜1か月）／ 生活が始まって ／ 長く住む ／ 困ったとき
- **理由**：在留資格で切ると、ユーザーは自分がどれに当てはまるかを先に理解しなければならない。
  時期で切れば、自分の居場所がすぐ分かる

### 個別の相談には答えない（2026-09-15）
- **決めたこと**：サイトは「制度の説明」と「公式窓口への案内」に徹する。
  「あなたの場合はこの在留資格が取れます」といった個別判断は書かない
- **理由**：個別の相談に答えることは、行政書士・弁護士の資格が必要な領域に踏み込むおそれがある
- **見直す条件**：専門家と組む体制ができたとき。**それまでは「診断機能を付けては」と繰り返し提案しない**

### 対応言語は3段階で増やす（2026-09-15）
- **決めたこと**：
  - 第1段階：やさしい日本語、英語
  - 第2段階：ベトナム語、中国語（簡体字）、ネパール語
  - 第3段階：インドネシア語、ミャンマー語、ポルトガル語、韓国語
- **理由**：在留者数の多さと「情報の必要度」は一致しない。中国・韓国は長く住む人の割合が高く日本語が読める人も多い。
  一方、直近1年で増えた人数はネパール・インドネシア・ミャンマー・ベトナムが多く、
  「住民登録をどうするか」という情報を最も必要としているのはこの層。フィリピンの方は英語で相当カバーできる
- **いきなり多言語にしない理由**：更新が追いつかず情報が古くなるのが最大のリスク。
  この分野では古い情報は無いより有害
- **未確認**：根拠にした在留者数は検索で得た二次情報。**公式（出入国在留管理庁）で取り直すまでサイトには載せない**

### 見た目は浮世絵の色づかい。ただし本文の下に模様は敷かない（2026-09-16）
- **決めたこと**：藍（プルシアンブルー）・生成りの紙・朱の3色でまとめ、ヘッダーに青海波の地紋、
  トップに富士と波の絵（自作のSVG）を置く。既存3サイトとは別の見た目にする
- **理由**：Tatsuroさんの希望（富嶽三十六景のような日本らしい背景）。外国人読者にとって
  「日本のサイトに来た」と分かる手がかりになる
- **絵は自分たちで描く**：外部の画像を読み込まない。読み込みが遅くならないため、
  また通信の遅い環境からも見られるようにするため。著作権の問題も起きない
- **本文の下に模様は敷かない**：模様が薄くても、文字の後ろにあると読みにくい。
  地紋はヘッダーとフッターの帯だけ。本文は必ず無地の紙の上に置く
- **絵に動きをつける（2026-09-16 追記）**：波はゆっくり横に流れ、日は静かに上下し光がまたたく。
  CSSだけで作り、JavaScriptは使わない。動くのはトップの絵だけで、本文には動きを入れない。
  端末の設定で「視差効果を減らす」を選んでいる人には動きを止める（動きが苦手な人・乗り物酔いしやすい人がいるため）
- **見直す条件**：読みにくいという声が出たとき

### 公開の構成は既存3サイトに揃える。Cloudflare は挟まない（2026-09-15）
- **決めたこと**：GitHub Pages に独自ドメインを直接つなぐ（`CNAME` ファイルのみ）。Cloudflare は使わない
- **理由**：既存3サイトと同じ形にすれば、運用の手順も、うまくいかないときの切り分けも1つで済む。
  Cloudflare を挟むと設定が1段増える
- **見直す条件**：アクセスが増えて表示速度や攻撃対策が問題になったとき

### アクセス解析と広告のIDは、既存サイトのやり方に揃える（2026-09-15）
- **決めたこと**：GA4 は新しい測定ID `G-44PECD16GK`。AdSense は既存サイトのID
  `ca-pub-5761092657360295` を流用し、`ads.txt` も同じ内容にする
- **運営者自身のアクセスは計測しない**：既存3サイトと同じく、`?ga=off` を一度開くと
  そのブラウザでは以後計測しない（`?ga=on` で解除、`?ga=status` で確認）。
  パソコンとスマホでそれぞれ1回開く必要がある

### 記事は題材リストを先に作り、公式で裏が取れるものだけ書く（2026-09-16）
- **決めたこと**：記事の題材を先に100本ぶん洗い出し（[docs/article-plan.md](docs/article-plan.md)）、
  その中から公式ページで裏が取れたものだけを書く。1記事2000字程度、出典と最終確認日を必ず載せる
- **公開ペース**：最初の5本（AdSense申請用）を先に出し、100本に達するまで1日1本。101本目からは週1本
- **理由**：毎日1本を100日続けると、公式情報で書ける題材が途中で尽き、薄い記事や推測混じりの記事が
  混じるおそれがある。姉妹サイトが AdSense で「有用性の低いコンテンツ」と判定された経緯もあり、
  この分野では量より質が効く。題材を先に出し切っておけば、尽きたときに止められる
- **題材が尽きたら止める**：勝手に薄い記事を作り続けない。新しい題材を足すときは、
  先に「公式で裏が取れるか」を確かめてから一覧に加える

### 公式ページの取得は GitHub Actions に任せる（2026-09-15）
- **決めたこと**：公式ページの本文取得は `fetch-official.yml`（手動実行）で行い、
  結果を `data/raw/` にコミットする。クラウドのセッションはそれを読んでデータを作る
- **理由**：クラウドのセッションは許可ドメインしか見られず、`moj.go.jp` に直接アクセスできない。
  Actions からは出られるので、取得と利用を分ける
- **あわせて**：`moj.go.jp` をクラウド環境の許可ドメインに追加することを検討中（2026-09-15 時点で未対応）

### ロゴとOGP画像は自分たちで描き、ページごとに作る（2026-09-16）
- **決めたこと**：ロゴ（富士・日・波）は SVG で自作し、`favicon.svg` / `favicon.ico` /
  `apple-touch-icon.png` / `assets/logo.svg` を [scraper/generate-brand-assets.js](scraper/generate-brand-assets.js)
  から書き出す。SNSに出る画像（OGP画像）は**ページごとに1枚**作り、題名を焼き込む
  （[scraper/generate-ogp-images.js](scraper/generate-ogp-images.js)、`assets/ogp/<ページ名>.png`）
- **理由**：共通の1枚だと、どのページを共有しても同じ絵になり、受け取った側は開くまで中身が分からない。
  題名が入っていれば「在留カードの記事だ」と分かる。外部の画像素材を使わないのは、
  読み込みを軽くするためと、著作権の問題を起こさないため
- **画像の作り方**：HTML を組んで、画面のないブラウザ（chrome-headless-shell）で 1200×630 に撮る。
  ふつうの Chrome を `--headless` で使うと**画像の下が切れる**（実際に起きた）ため、専用版を使う。
  追加のパッケージは入れない
- **見張り**：ページが指している画像が実在するか、大きさが 1200×630 か、ページの名前と画像の名前が
  ずれていないかを `npm test` で確かめる（`test/brand.test.js`）

### 記事の毎日1本は、GitHub Actions が書いて、検査に通ったものだけ公開する（2026-09-16）
- **決めたこと**：`.github/workflows/publish-article.yml` が毎日 日本時間6:00 に動き、
  [data/article-queue.json](data/article-queue.json)（100本の題材）の上から1本を書く。
  書いたあと [scraper/lib/article-guards.js](scraper/lib/article-guards.js) の検査にかけ、
  **通ったものだけ**を保存して公開する。落ちたら3回まで書き直させ、それでも駄目なら**その日は公開しない**
- **検査の中身**：引用が公式ページの本文に一字一句あるか／本文の数字が同じ節の引用にあるか／
  個別の判断を述べる表現が無いか／英語と日本語が揃っているか／最後の節が公式窓口の案内になっているか
- **理由**：「公式に無いことを書かないで」と頼むだけでは守られない。このリポジトリでも、
  最初の5本を書いたときに3回、頼んだはずのことを外した。頼み方ではなく検査で守る
- **題材が出典待ちのときは書かない**：出典が `data/sources.json` に登録され、`data/raw/` に本文が
  保存されている題材だけを選ぶ。無理に書かせない
- **ペース**：100本に達するまで毎日1本、101本目からは週1本（月曜）。判定は
  `scraper/write-next-article.js` の中にあり、テストで見張っている
- **費用**：呼び出しごとに `data/usage-log/YYYY-MM.json` に積み上げる（既存3サイトと同じ考え方）
- **必要な設定**：リポジトリの Actions secrets に `ANTHROPIC_API_KEY`
- **書けなかった題材は飛ばす（2026-09-16 追記）**：3回書き直しても検査に通らない題材には
  `status: "blocked"` と理由を書き込み、翌日は次の題材へ進む。飛ばさないと、同じ題材で毎日止まり、
  後ろの題材が永久に出ない。たいていの原因は「出典のページにその話が書かれていない」ことなので、
  出典を足し直したら `pending` に戻す

### 右下に「どこから始める？」の道案内を置く（2026-09-16）
- **決めたこと**：全ページの右下に追従ボタンを置き、**2問**（いまどの段階か → 何を知りたいか）で
  該当ページへ案内する。あわせて「在留資格から探す」（33件の選択）と、記事・在留資格の一覧への入口を置く
  （[scraper/lib/guide-widget.js](scraper/lib/guide-widget.js)）
- **理由**：在留資格33件＋記事100本になると、目当ての情報にたどり着けない。読者は日本語が読めず、
  「自分が何を知らないか」も分からないことが多いので、段階から絞り込める入口が要る。
  既存3サイトの診断ボタンと同じ考え方だが、**質問は2問まで**（言葉の壁があるぶん、質問を重ねると離脱する）
- **道案内がやらないこと**：個別の判断（「この在留資格が取れる」など）。案内するのは
  「制度を説明したページ」だけで、最後に必ず公式窓口への断りを出す
- **リンク切れを作らない**：まだ書いていない題材はリンクにせず「準備中（Coming soon）」と出す。
  リンク先が実在するか、100本の題材がちょうど1か所ずつ入っているかを `npm test` が見張る
- **狭い画面では英語だけ残す**：読者は英語で読む人が中心のため、日本語のほうを省く

### 新しい公式ページは「すでにある公式ページのリンク」から増やす（2026-09-16）
- **決めたこと**：出典を増やすときは、URLを推測せず、**取得済みの公式ページに載っていたリンク**
  （`data/raw/*.links.txt`）から取る。機関のトップページを先に登録して、そこから個別ページを拾う
- **手順**：`data/sources.json` に登録 → `fetch-official.yml` で本文を取得 →
  **本文に題材の言葉が実在するかを確かめる** → そこではじめて題材（`data/article-queue.json`）に割り当てる
- **理由**：URLを推測すると、存在しないページや、題材と関係ないページを出典にしてしまう。
  リンクから辿れば、公式が自分で示したページだけを使える
- **公式ドメインの一覧**（[scraper/lib/schema.js](scraper/lib/schema.js)）は、必要になった機関だけ足す。
  使う予定の無い機関を先に広げない

### トップは「用事ごとのアイコンタイル」にする（2026-09-16）
- **決めたこと**：トップの入口を、時期（来日直後・長く住む）ではなく**用事**で9つに分ける
  （[scraper/lib/categories.js](scraper/lib/categories.js)）。タイルはアイコン主体で、
  文字は「英語の短い語＋日本語＋件数」だけ。説明の文章はタイルに入れない
- **理由**：探す人は「在留カード」「健康保険」と用事で探す。自分が今どの時期かは本人にも分かりにくい。
  また、以前のトップは1画面目がほぼ文字で、5枚中3枚が「準備中」だったため、空の印象が先に立っていた
- **件数を出す**：「記事2本」「33件」のように数字を見せる。中身の無いタイルはリンクにせず「準備中」と薄く出す
  （押しても何も無いページに飛ばさない）。リンク先が実在するかは `npm test` が見張る
- **記事一覧も同じ9分類でまとめる**：タイルから `/guide/#health` のように飛ぶ。分類はここ1か所で決める
- **書く順番も分類で決める**：毎日の自動更新は「記事の少ない分類」から書く。題材リストの順のままだと
  タイルが1つずつしか埋まらず、何週間も「準備中」が並ぶため
- **トップの絵は横長の帯にする**：以前は絵だけで最初の画面が埋まり、入口が下に押し出されていた。
  富士は少し左に寄せ、スマホで横が切れても見えるようにした。動きの決めごと（ゆっくり・prefers-reduced-motion で停止）はそのまま

### プライバシーポリシーとFAQを置く（2026-09-16）
- **決めたこと**：`/privacy/` と `/faq/` を [scraper/generate-static-pages.js](scraper/generate-static-pages.js)
  から書き出し、全ページのフッターから行けるようにする。文面は既存3サイトに揃え、英語と日本語を並べる
- **理由**：広告（AdSense）と解析（GA4）は Cookie を使うため、その扱いを説明したページが要る。
  審査でも見られる。既存3サイトには最初からあったが、このサイトには無かった
- **このサイト固有の点**：FAQ で「私はどの在留資格が取れますか」に**答えない**ことをはっきり書く。
  個別の申請についての判断は資格を持つ専門家の業務であり、サイトは制度の説明と公式窓口の案内に徹する
- **見張り**：広告・解析・連絡先・計測をやめる方法（`?ga=off`）が書かれているか、
  フッターから両方のページに行けるか、サイトマップに入っているかを `npm test` で確かめる

### settle-in-japan.net をクラウドの許可ドメインに入れた（2026-09-16）
- **決めたこと**：クラウドのセッションから公開サイトを見られるようにした。
  `npm run verify-live` で、main に反映した内容が実際に公開されているかを確認できる
- **あわせて**：クラウドから外に出るときはプロキシを通るため、`verify-live.js` を
  プロキシ経由（CONNECT）に対応させた。対応前はすべて 403 になっていた
- **確認できるようになったこと**：ページが開くか／解析と広告のタグ／サイトマップの網羅／
  出典と最終確認日／OGP画像が実在するか／ads.txt・robots.txt・ロゴ

### 広告は自動広告を使わず、置き場所をこちらで決める（2026-09-16）
- **決めたこと**：AdSense の自動広告は使わず、[scraper/lib/ads.js](scraper/lib/ads.js) で決めた
  4か所にだけ広告ユニットを置く（記事の本文前・本文後／在留資格ページの説明後／トップのタイル下）。
  番号は [data/ad-slots.json](data/ad-slots.json) に書く。入れ方は [docs/ad-setup.md](docs/ad-setup.md)
- **理由**：このサイトには公式ページの**引用**が載っている。自動広告は引用のすぐ横や途中にも広告を入れるため、
  「どれが公式の文言で、どれが広告か」が読む人に分からなくなる。法律・手続きの情報でそれが起きると誤解のもとになる
- **線引き**：節の中には入れない（各節は引用で終わるため）／1ページに2つまで／必ず「広告」と分かる表示を添える
- **番号が空のあいだは枠を書き出さない**：審査中でもそのまま公開できる。番号を入れた日から出る
- **見張り**：`npm test` が、節の中に広告が入っていないか、引用とくっついていないか、
  1ページ2つを超えていないか、番号が空のときに空欄が出ていないかを確かめる
